import React from "react";
import { useQuery, gql } from "@apollo/client";

type SheetListProps = {
  setId: number;
};

const SheetList: React.FC = () => {
  return <div>This is the sheetlist</div>;
};

export default SheetList;
